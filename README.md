# Guía rápida de git

`git init`

`git status`

```
    git add .
    git add <file>
```

`git commit -m "<commit message>"`

```
    git branch
    git branch -M main
```

`git remote add origin <remote url>`

`git push -u origin main`